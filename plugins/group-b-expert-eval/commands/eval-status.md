---
description: 查看 B 组评测工作区当前状态：对象、case 数、运行与证据概况
---

读取评测工作区（默认 `eval/`，可用参数指定其他路径），汇总并展示：

1. 已接入的被测对象（单专家 / 专家团）及其版本与快照哈希；
2. case 定义数量与任务类型分布（结构化 / 混合 / 开放）；
3. 已完成运行次数、正式 / 异常运行分布；
4. 最近一次的证据与评分摘要；
5. 若有未决人工建议或未复测的优化副本，逐项列出。

只读操作：不得修改任何 case、证据或专家包文件；缺失的项明确标注"未配置"，
不要推测或虚构数据。
